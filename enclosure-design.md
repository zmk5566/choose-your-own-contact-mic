# 3D 外壳设计要点
## Enclosure Design Notes

---

## 一、设计目标

1. **三轨制通用**：同一个外壳能装下基础款（空腔）、PiP 款（buffer 小板）、P48 款（前级板，体积更大）
2. **3D 打印件即模具**：环氧灌进去就是最终成品，不脱模
3. **手感好**：参与者会反复贴到不同物体表面，外形要顺手
4. **抗应力**：线缆出口必须做应力释放，否则反复弯折会断线

---

## 二、形状选择

推荐 **圆柱形 puck（曲棍球饼形）**：

```
        ┌─────────────────────┐
        │                     │  ← 顶面：暴露 piezo（贴物面）
        │  ◯ piezo (27mm)     │
        │                     │
        ├─────────────────────┤
        │                     │
        │   扩展腔（容纳 PCB）  │  ← 内腔：放电路板
        │                     │
        │                     │
        ├────────────��────────┤
        │ ←━━━ 出线孔（带应力释放）
        └─────────────────────┘
```

**关键尺寸**（按 27mm piezo 设计）：

| 维度 | 数值 | 说明 |
|---|---|---|
| 外径 | 35mm | piezo + 4mm 壁厚 |
| 总高 | 18mm | 容纳 PCB + 灌胶层 |
| piezo 凹槽深 | 0.6mm | piezo 黄铜片厚度 |
| piezo 凹槽径 | 27.5mm | 略大于 piezo |
| 内腔深 | 12mm | 容纳最大尺寸 PCB（P48 板） |
| 内腔径 | 28mm | |
| 出线孔径 | 4mm | 配合 3mm 屏蔽线 + 1mm 应力释放空间 |
| 壁厚 | 2mm | PLA/PETG 都够 |

> **注意**：如果你的 P48 板尺寸 > 28mm × 12mm，先量好实物再调整内腔。

---

## 三、关键设计细节

### 1. piezo 凹槽
- 设计成**比 piezo 略大 0.5mm**，让 piezo 能轻松放入
- 凹槽**底面平整**，piezo 黄铜面朝外（贴物体的那面）
- piezo 银面（焊点面）朝内，焊线从这里走

### 2. 出线孔的应力释放
```
内腔 ──┐
        │   ╱─── 锥形扩张
        │  ╱        ↓
        │ ╱   线在这里走
        │╱     ↓
        ┴────── 外部
```
- 内侧做成**锥形扩张**（喇叭口），半径 1mm
- 线缆穿过后用热缩管包一段���热缩管被锥形腔卡住，形成应力释放
- 如果不做这一步，线缆从外部弯折时直接拽到焊点，三五次就断

### 3. 灌胶口
- **顶部不灌胶**（piezo 表面要直接接触物体）
- 从**侧面或底部**预留一个小孔灌入环氧（直径 5mm）
- 或者：**设计成两件式**，先把 piezo + PCB 放进去，再用 5min 环氧把背面整个封住

**推荐两件式**：
```
Part A（杯体）：含 piezo 凹槽 + 内腔 + 出线孔
Part B（盖子）：扣在 Part A 背面，环氧从此面灌
```

### 4. 防转结构
- 内腔底部加一个**小柱子或卡槽**，让 PCB 嵌进去后不会乱转
- piezo 银面焊点位置和 PCB 焊点位置要对齐

### 5. 标识
- 顶部边缘刻**编号或参与者名字位**（凹槽 5mm × 15mm × 0.5mm）
- 让大家用记号笔自己写名字，或贴贴纸

---

## 四、文件结构建议

```
hardware/
├── enclosure_v1.scad        # OpenSCAD 源文件（可参数化）
├── enclosure_v1.stl         # 导出给打印机
├── enclosure_v1_lid.stl     # 盖子（如果两件式）
└── README.md                # 打印参数
```

**OpenSCAD 参数化推荐**：留 `piezo_diameter`、`pcb_height`、`shell_thickness` 三个参数，未来换 piezo 尺寸或换 PCB 不用重画。

---

## 五、打印参数

| 参数 | 值 | 备注 |
|---|---|---|
| 材料 | PETG（首选）/ PLA | PETG 韧性好不易脆 |
| 层高 | 0.2mm | 平衡速度和质量 |
| 填充 | 30% gyroid | 兼顾轻量与强度 |
| 壁厚（perimeters） | 3 | ~1.2mm 实壁 |
| 顶/底层数 | 4 | |
| 支撑 | 否 | 设计成不需要支撑 |
| 颜色 | 黑色 / 半透明 | 半透明可看到内部电路，更有"DIY 感" |
| 单个时间 | ~30min | |

**25 个分两批打印**：第一批 13 个 + 第二批 12 个，避免一次失败全军覆没。

---

## 六、备选方案：极简版（不用 3D 打印）

如果 3D 打印来不及，备用方案：

1. **薄铁罐头盖** + 热熔胶 → 屏蔽 + 拾振一体
2. **塑料瓶盖** + 热熔胶 → 最廉价
3. **木块挖槽** + 蜡封 → 手工感最强

但这些都损失了"扩展腔"，进阶款做不了。3D 打印仍是首选。

---

## 七、TODO

- [ ] 用 OpenSCAD / Fusion 360 画第一版模型
- [ ] 打 1 个原型，实际试装一个 piezo + 一块 PCB，验证尺寸
- [ ] 调整后，批量打印 25 个
- [ ] 测试灌胶流程：环氧能不能从侧孔/盖子灌满，30min 能不能 handle
- [ ] 准备一份**外壳装配示意图**（一页 A4），现场发给参与者
